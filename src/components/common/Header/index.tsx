import { NavLink } from "react-router-dom";

const Header = () => {
  return (
    <header className="rz__header">
      <NavLink to="/">POC BOT BUILDER</NavLink>
    </header>
  );
};

export default Header;
